const HELPERS = {
    _TIME_PER_CHECK_OVERLOAD: 50000,
}

const MODELS = {
    BLOG: {
        COLLECTION_NAME: 'Blogs',
        DOCUMENT_NAME: 'Blog',
    },
}

module.exports = {
    HELPERS,
    MODELS,
}
